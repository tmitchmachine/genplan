import 'package:flutter/material.dart';

class Location extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Location Preferences'),
      ),
      body: Center(
        child: Text('Location update functionality will be here.'),
      ),
    );
  }
}
